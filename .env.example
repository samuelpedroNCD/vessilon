# Vessilon environment — copy to .env.local and fill in.
# Supabase project settings → API. Use the publishable (sb_publishable_...) key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secrets (do NOT prefix with NEXT_PUBLIC). Add when needed:
# SUPABASE_SERVICE_ROLE_KEY=
