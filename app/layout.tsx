import type { Metadata, Viewport } from "next";
import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";
import "./globals.css";

export const metadata: Metadata = {
  title: "Vessilon — The operating system for yacht brokerage",
  description:
    "Vessilon is the fleet-first, AI-native platform that unifies the yacht brokerage — listings, leads and deals across every line of business, brochures, marketing and owner reporting — on a single source of truth where the yacht is the primary object.",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${GeistSans.variable} ${GeistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
